# Origin of the API server. Leave EMPTY in production so the SPA calls
# the same Vercel deployment (recommended). For local development set
# this to your local Express server:
#
#   VITE_API_BASE=http://localhost:5000

VITE_API_BASE=
